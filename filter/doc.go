// Package filter defines the frame transform contracts: filters that resize,
// resample, or convert decoded frames through caller-owned result buffers,
// the capability descriptors the planner and shape solver select adapters by,
// and the per-runtime registry external filters plug into.
package filter
