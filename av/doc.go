// Package av is the transport-neutral media vocabulary every other package
// speaks: packets, frames, events, and streams, plus the identifiers (media,
// codec, format, protocol), rational timestamps, and the buffer-ownership
// declarations the pipeline's copy machinery relies on. It depends on nothing
// in goav and changes least.
package av
